{
    "builds": [{
        "src": "exercicio/wsgi.py",
        "use": "@vercel/python",
        "config": {"maxLambdasize": "15mb"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "exercicio/wsgi.py"
        }
    ]
}
